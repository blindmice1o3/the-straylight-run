<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rl_notes_viewer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".accelerometer.game.notes.topics.NotesViewerFragment">

    <TextView
        android:id="@+id/tv_close_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:background="@color/yellow"
        android:padding="8dp"
        android:text="@string/text_x"
        android:textColor="@color/blue"
        android:textSize="24sp" />

    <!-- TODO: Update blank fragment layout -->
    <!--
    <ScrollView
        android:id="@+id/sv_zoom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">


        <ImageView
            android:id="@+id/iv_notes"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:scrollbars="vertical"
            android:src="@drawable/harvest_moon_natsume" />
    </ScrollView>
    -->
</RelativeLayout>